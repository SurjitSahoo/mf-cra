{
  "name": "root",
  "private": true,
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start --scope=example*"
  },
  "devDependencies": {
    "lerna": "^10.0.0"
  }
}